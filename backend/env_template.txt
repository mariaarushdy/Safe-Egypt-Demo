# Copy this file to .env and fill in your values
# Database Configuration
DB_NAME=safe_egypt_db
DB_USER=postgres
DB_PASSWORD=your_database_password
DB_HOST=localhost
DB_PORT=5432

# Alternative: Full Database URL (SQLAlchemy format)
DATABASE_URL=postgresql://postgres:your_password@localhost:5432/safe_egypt_db

# JWT Authentication
SECRET_KEY=your-super-secret-jwt-key-change-this-in-production-use-random-string

# Google AI (Gemini)
GOOGLE_API_KEY=your_google_api_key

# Application Settings
DEBUG=True
PORT=8000

# CORS Settings (for frontend)
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# File Upload Settings
UPLOAD_FOLDER=data/uploads
MAX_UPLOAD_SIZE=50  # MB
ALLOWED_IMAGE_EXTENSIONS=jpg,jpeg,png,gif,webp
ALLOWED_VIDEO_EXTENSIONS=mp4,avi,mov,wmv

# Token Settings (optional, defaults are provided)
ACCESS_TOKEN_EXPIRE_MINUTES=1440  # 24 hours
REFRESH_TOKEN_EXPIRE_DAYS=30

